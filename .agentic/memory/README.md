# Memory ledgers

Mutable runtime state (trust, usage, capacity, scheduler, breakers,
decisions, STATE.md) lives here. Created on demand; git-ignored so the
distributable repository stays free of local artefacts.
